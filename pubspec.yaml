name: agora_token_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  agora_rtc_engine: ^1.0.12
  permission_handler: ^5.0.1
  screenshot_and_share: ^0.0.1
  http: ^0.12.2 # for receiving token from server

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/bg.jpg
    - assets/dp.jpeg
